export default function Page() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-pulse-wash px-6 text-pulse-ink">
      <section className="w-full max-w-2xl">
        <p className="text-sm font-semibold uppercase tracking-wide text-pulse-teal">PulseCRM</p>
        <h1 className="mt-3 text-4xl font-black tracking-normal text-pulse-ink">Frontend implementation base</h1>
        <p className="mt-4 max-w-xl text-base leading-7 text-pulse-muted">
          This workspace is ready for UI implementation. Build each page from the documented development process, with reusable
          components, realistic mock data, services, adapters, and clean feature folders from the start.
        </p>
      </section>
    </main>
  );
}
